# mental-health-chatbot-using-seq2seq-model
A mental health chatbot using a Seq2Seq model that generates appropriate responses to user messages. The project involves text preprocessing, tokenization, padding, word embeddings, encoder-decoder architecture, and sequence generation to create conversational responses.
